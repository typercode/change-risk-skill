# 使用方式

在需要上线前风险评估时，给出以下信息，要求按 `change-risk-checklist` skill 输出报告。

## 强制门控

`线上生效配置文件或 profile 名称` 是**硬门控**：不提供则评估中止，需要明确说"我没有生产配置，继续评估"才能放行（放行后相关章节统一打 `⚠️ 待确认 - 缺生产配置`）。

推荐在提示词中把这一项**单独成行**（如下方模板所示），让用户每次都明确给出或明确跳过。

## 推荐提示词

```markdown
请使用 change-risk-checklist 对本次 Java/Spring Boot 服务变更做生产发布风险评估。

项目：
基线版本（线上分支/tag/commit）：
目标版本（待发布分支/tag/commit）：
线上生效配置文件或 profile 名称（**必填**，硬门控；缺则评估中止；如无配置请明确说"我没有生产配置，继续评估"）：
部署环境补充信息（如实例数、JVM 内存、DB/Redis 限额、网络隔离、监控平台）：

重点关注：
- 对外接口兼容性
- 线上配置是否误连 beta/sit/dev/test/localhost
- 定时任务、连接池、线程池、超时、重试、资源压力
- DB/Redis/Tomcat/JVM/日志配置
- 监控埋点和告警待确认项
- 安全风险、明文密钥、鉴权缺失、敏感日志
- 数据库和配置回滚方案

输出要求：
- 先列阻断级和高风险项，再给检查明细
- 每个结论都给出证据路径、配置键、类名或接口路径
- 代码无法验证的平台项标记为“⚠️ 待确认”，不要当作通过
- 同时列出正面改进
```

## 最小输入

如果信息不全，至少提供：

- 基线版本
- 目标版本
- 线上配置文件或 profile 名称（**硬门控**；缺则中止）

`线上配置文件或 profile 名称` 不能用"标记为待确认"绕过——见上文"强制门控"。

其他缺失信息（实例数、JVM、DB/Redis 限额等）由评估报告标记为 `⚠️ 待确认`。
