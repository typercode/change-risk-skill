---
name: change-risk-checklist
description: Use when 评估 Java 或 Spring Boot 服务生产发布前的代码变更风险，尤其是上线评估、变更风险、发布风险、分支/tag 对比、生产配置、兼容性、回滚、可观测性、安全或运维风险检查。
---

# 变更风险检查清单

## 概览

从代码 diff、配置、依赖和运维控制面评估生产发布风险。核心原则：区分已验证证据和推测。

## 使用场景

用于 Java / Spring Boot 发布评审、分支或 tag 对比、生产配置核查，以及用户提到变更风险、上线评估、发布风险、配置核查、回滚方案、兼容性检查等场景。

除非用户明确要求发布风险评估，不要用于普通代码风格评审或本地问题调试。

## 必要输入

> 🚦 **生产生效配置文件或 profile 名称**是**硬门控**（详见下方"输入门控"）。缺它会污染 5.1/5.2/6.6/8.1–8.4/9.4/12.5 的评估结论，必须先提供或显式 bypass。

只询问无法推断的缺失信息：

- 基线版本：线上分支、tag 或 commit
- 目标版本：待发布分支、tag 或 commit
- 生产生效配置文件或 profile 名称（**硬门控**，见下方"输入门控"）
- 必要的部署上下文：实例数、JVM 内存、DB/Redis 限额、网络隔离、监控平台

其他必要输入（基线/目标/部署上下文）信息不全时，先评估可验证部分，并把受影响检查项标记为 `⚠️ 待确认`。

## 输入门控

缺生产生效配置会污染 5.1/5.2/6.6/8.1–8.4/9.4/12.5 等章节的评估质量。处理规则：

- **硬停止 + 询问（默认）**：缺配置则中止评估，要求补充。
- **显式 bypass**：用户明确说"我没有生产配置，继续评估"时放行；放行后所有受影响章节统一打 `⚠️ 待确认 - 缺生产配置`，**不得伪造证据**。
- **可接受的输入形式（任一即可）**：
  1. Spring profile 名称（如 `prod-ca-gd`），按命名约定 `application-{profile}.{properties,yml,yaml}` + `filter-{profile}.properties` 解析
  2. 具体文件路径（绝对或相对仓库根）
  3. "profile 名称 或 文件路径 任一"（用户明确二选一时）
  4. 配置内容直接粘贴（多行 properties / yaml 块）
- **路径或 profile 无法定位时**：仍硬停，提示用户改用"配置内容直接粘贴"形式或显式 bypass。

## 执行流程

0. **生产配置门控**：按"输入门控"规则检查用户是否提供生产生效配置（profile 名称、文件路径或内联内容）。未提供则硬停止并询问；用户明确说"我没有生产配置，继续评估"则放行并打标 `⚠️ 待确认 - 缺生产配置`。
1. 通过 `git diff --stat`、变更文件列表和关键配置文件确定范围。
2. 先读取生产生效配置，再判断环境配置风险。
3. 沿调用方、运行时装配和配置引用追踪变更代码路径。
4. 套用 [change-risk-checklist.md](change-risk-checklist.md) 的详细检查项。
5. 先报告阻断级问题，再输出完整检查明细。

## 证据规则

| 结果 | 使用条件 |
|---|---|
| `✅ 通过` | 证据直接证明该项安全或未变化。 |
| `❌ 不通过` | 证据表明存在真实违规、回归或缺少必要控制。 |
| `⚠️ 待确认` | 代码无法证明、需要外部平台数据，或缺少发布上下文。 |
| `N/A 不适用` | 该项与当前服务或变更范围无关。 |

每个非 `N/A` 结论都必须给出具体证据：文件路径、配置键、类名、接口路径、依赖名称或执行过的命令。

降级模式（缺生产配置）下，5.x/6.6/8.x/9.4/12.5 章节的结论统一打 `⚠️ 待确认 - 缺生产配置`，证据栏写"N/A（无法验证 - 缺生产配置）"，不当作通过处理。

## 严重程度

| 严重程度 | 使用条件 |
|---|---|
| `🔴 阻断级` | 发布前必须修复，或由负责人明确接受风险。 |
| `🟠 高风险` | 强烈建议发布前修复。 |
| `🟡 中风险` | 建议优化或纳入跟踪。 |
| `🔵 低风险` | 可作为后续改进。 |

出现安全暴露、生产误连非生产系统、不兼容的公开 API 删除、运维端点缺少鉴权、不可逆 DB 变更且无回滚方案、核心行为变更且无回滚方案时，提升为 `🔴 阻断级`。

## 检查重点

优先关注 API 兼容性；生产配置中混入 `beta` / `sit` / `dev` / `test` / `localhost`；定时任务、消费者、连接池、线程池、重试、超时；DB/Redis/HTTP/Tomcat/JVM/日志变更；监控指标；密钥和敏感日志；端点鉴权；DDL 和配置回滚。

## 输出模板

按以下顺序输出：变更概要 → 🔴 阻断级问题 → 🟠 高风险问题 → 🟡/🔵 中低风险 → ⚠️ 待确认事项 → ✅ 正面改进 → 检查明细。

每个问题包含严重程度、问题描述、证据、影响和建议动作。

## 常见错误

- 没有平台证据就把平台项标为通过。应标记为 `⚠️ 待确认`。
- 只看变更文件。还要追踪调用方、配置、生成产物和运行时装配。
- 认为生产配置中的 test/dev 关键词无害。
- 报告风险但不给精确文件、配置、类或接口证据。
- 把阻断级问题埋在完整检查表里，而不是优先列出。
- 未先获取生产配置就评估 5.x/6.6/8.x/9.4/12.5 章节，输出虚假通过结论。正确做法：触发生产配置门控，缺则停或显式 bypass。
