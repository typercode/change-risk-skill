---
name: change-risk-checklist-reference
description: Detailed checklist reference for Java and Spring Boot production release risk assessment.
---

# 系统变更风险评估 Checklist

> 适用范围：Java / Spring Boot 技术栈项目
> 主入口：使用同目录 [SKILL.md](SKILL.md) 执行评估流程，本文件作为详细检查表。
> 评估结果状态：✅ 通过 | ❌ 不通过 | ⚠️ 待确认 | N/A 不适用
> 严重程度：🔴 阻断级（必须修复才能上线）| 🟠 高风险（强烈建议修复）| 🟡 中风险（建议优化）| 🔵 低风险（可后续改进）

## AI 评估执行指引

执行变更风险评估时，遵循以下原则：

### 评估范围确定
- 用户会指定两个分支/tag 进行对比（如 `online-2024-10-25` → `master`），或指定一组变更文件
- 用户会指定线上使用的 filter 配置文件（如 `filter-ca-gd.properties` + `application-ca-gd.properties`），以此确定线上实际生效的配置值
- 先通过 git diff 或文件对比确定变更范围，再逐项检查
- 缺少分支、配置、平台信息时继续评估可验证部分，并将受影响检查项标记为 `⚠️ 待确认`

### 检查项职责划分
- **AI 自主完成**：通过读取代码、配置文件、pom.xml、logback.xml、启动脚本等可直接给出结论的检查项
- **AI 辅助 + 人工确认**：AI 列出变更事实和潜在风险，标记为 `⚠️ 待确认`，由研发/业务自行确认（如 1.1、1.2 上下游影响确认）
- **纯人工确认**：AI 无法从代码获取信息的项，标记为 `⚠️ 待确认` 并注明需要确认的具体内容（如网络隔离策略、监控平台告警配置）

### 配置门控

执行本 checklist 前必须先通过 `SKILL.md` 步骤 0 的生产配置门控。门控通过后，5.1/5.2/6.6/8.1–8.4/9.4/12.5 章节才进入正常评估；门控 bypass 后这些章节统一打标 `⚠️ 待确认 - 缺生产配置`，证据栏写"N/A（无法验证 - 缺生产配置）"。

受影响章节清单（不接受 AI 自行扩展，避免执行不一致）：

- 5.1 线上配置不含非线上环境地址
- 5.2 网络隔离
- 6.6 Redis 密码加密
- 8.1–8.4 DB 连接配置
- 9.4 日志敏感信息
- 12.5 密钥/凭据加密存储

`✅ 通过` 在降级模式下视为不成立（即使代码看起来 OK，配置值未验证）。

### 证据和输出规则
- 每个检查项必须填写“结果”和“备注”
- `✅ 通过` 必须有代码、配置、diff 或用户提供材料支撑
- `❌ 不通过` 必须列出具体证据和可执行修复建议
- `⚠️ 待确认` 必须说明需要谁/哪个平台/哪类数据来确认
- 备注中列出具体的代码证据（文件路径、配置值、类名、接口路径、依赖名称等）
- 评估总结按严重程度分类汇总所有非通过项，并给出具体修复建议
- 同时列出本次变更中值得肯定的正面改进

### 项目技术栈约定
- 环境区分：通过 `beta`、`sit`、`dev`、`prod` 等关键字区分环境，线上配置中出现 beta/sit/dev 地址即为风险
- 应用监控 SDK：`metrics-ump-client`，埋点方式包括：
  - `@Metrics(types = {Type.QPS, Type.RT, Type.SUCCESS_RATE})` 注解
  - `MetricsClient.newInstance("应用名", "埋点描述")` 手动埋点
  - `MetricsLoggerFactory.init()` 初始化（采集 JVM 指标）
- 核心业务指标统一为：请求量（QPS）、耗时（RT）、成功率（SUCCESS_RATE）
- 告警配置在监控平台侧，代码中无法检查，相关项标记为 `⚠️ 待确认，需查看监控平台`
- 密码加密：Jasypt（`ENC(...)`）或 Druid（`config.decrypt=true`）
- 公司定制容器：`bjca-footstone-shaded-spring-boot-web-runtime-all`
- 公司日志框架：`bjca-footstone-shaded-logback`
- 公司 HTTP 客户端：`bjca-footstone-shaded-httpclient-all`
- 网络 IP：`192.168.20.6:8081` 是生产环境的内网 API 网关负载地址

---

## 一、变更影响评估

> 目的：确认变更对上下游系统的影响，确保联动上线和异常处理机制到位。  
> AI 职责：列出变更了哪些对外接口、新增/删除了什么、哪些有潜在风险。1.1 和 1.2 标记为 `⚠️ 待确认` 由研发/业务自行确认。

| # | 检查项 | 严重程度 | 结果 | 备注 |
|---|--------|---------|------|------|
| 1.1 | 是否评估了对业务方和调用系统的影响，并与相关方确认联动上线方案（研发/业务确认） | 🔴 阻断级 | | |
| 1.2 | 是否已周知业务方和调用方本次变更内容及上线时间（研发/业务确认） | 🔴 阻断级 | | |
| 1.3 | 是否制定了上线异常处理机制（回滚方案、降级策略、应急预案） | 🔴 阻断级 | | |

---

## 二、向后兼容性检查

> 目的：确保变更不会破坏现有调用方的正常使用。

| #   | 检查项                          | 严重程度 | 结果 | 备注 |
|-----|------------------------------|---------|------|------|
| 2.1 | 是否删除或重构了对外接口                 | 🔴 阻断级 | | |
| 2.2 | 是否移除或更改了已有字段、类、接口的签名或语义      | 🔴 阻断级 | | |
| 2.3 | 是否更改了默认行为（如默认配置值、处理逻辑、返回结构等） | 🟠 高风险 | | |
| 2.4 | 分析对外接口的变更，是否向后兼容             | 🔴 阻断级 | | |

---

## 三、定时任务配置核查

> 目的：确保定时任务不会对系统资源产生过大压力，执行频率合理。

| # | 检查项 | 严重程度 | 结果 | 备注 |
|---|--------|---------|------|------|
| 3.1 | 定时任务的执行时间是否合理（避开业务高峰期） | 🟠 高风险 | | |
| 3.2 | 定时任务是否会对数据库等资源产生过大压力 | 🟠 高风险 | | |
| 3.3 | 定时任务的间隔设置是否合理（不应秒级，需根据业务合理设置） | 🟠 高风险 | | |

---

## 四、HTTP 连接池配置检查

> 目的：确保 HTTP 连接池配置合理，避免连接泄漏或资源耗尽。

| # | 检查项 | 严重程度 | 结果 | 备注 |
|---|--------|---------|------|------|
| 4.1 | 是否正确配置了 HTTP 连接池参数 | 🟠 高风险 | | |
| 4.2 | 最大连接数和每路由连接数设置是否合理 | 🟠 高风险 | | |
| 4.3 | 是否配置了空闲连接回收策略 | 🟡 中风险 | | |
| 4.4 | 是否有防止连接池资源耗尽的保护机制（超时、重试限制等） | 🟠 高风险 | | |

---

## 五、业务请求地址检查

> 目的：确保线上环境不会误连非线上环境，所有地址可配置化。  
> **前提：生产配置门控已通过**（见上文"配置门控"）。未通过则 5.1/5.2 结论统一为 `⚠️ 待确认 - 缺生产配置`。
> AI 检查方式：扫描线上 filter 配置文件中的所有 URL，检查是否包含 `beta`、`sit`、`dev`、`test`、`localhost`、`127.0.0.1` 等非线上标识。扫描 Java 代码中的硬编码 URL（`http://`、`https://`）。

| # | 检查项 | 严重程度 | 结果 | 备注 |
|---|--------|---------|------|------|
| 5.1 | 线上配置（配置文件、Apollo、K8s ConfigMap）中不包含非线上环境地址（检查 beta/sit/dev/test 等关键字） | 🔴 阻断级 | | |
| 5.2 | 线上环境网络策略是否隔离了非线上环境（待人工确认网络拓扑） | 🔴 阻断级 | | |
| 5.3 | 代码中是否存在硬编码的请求地址 | 🟠 高风险 | | |
| 5.4 | 所有外部请求地址是否均支持配置化（通过 `@Value` 或配置文件注入） | 🟠 高风险 | | |

---

## 六、Redis 配置检查

> 目的：确保 Redis 连接池配置合理，连接安全可靠。  
> **前提：生产配置门控已通过**（见上文"配置门控"）。未通过则 6.6 结论统一为 `⚠️ 待确认 - 缺生产配置`。

| # | 检查项 | 严重程度 | 结果 | 备注 |
|---|--------|---------|------|------|
| 6.1 | 最大连接数设置是否合理（根据 Redis 服务器配置，通常 8-32） | 🟠 高风险 | | |
| 6.2 | 最大空闲连接设置是否合理（最大连接数的 50%-80%） | 🟡 中风险 | | |
| 6.3 | 连接超时设置是否合理（3-10 秒） | 🟠 高风险 | | |
| 6.4 | 连接池是否配置了重试机制 | 🟡 中风险 | | |
| 6.5 | 是否有连接池监控和健康检查（testOnBorrow/testWhileIdle） | 🟡 中风险 | | |
| 6.6 | 是否配置了 Redis 密码且密码加密存储 | 🔴 阻断级 | | |

---

## 七、Tomcat 配置检查

> 目的：确保 Web 容器配置满足线上运行要求。

| # | 检查项 | 严重程度 | 结果 | 备注 |
|---|--------|---------|------|------|
| 7.1 | 是否使用了公司定制的 Tomcat 容器 | 🟠 高风险 | | |
| 7.2 | Tomcat 线程池配置是否合理（max-threads、min-spare-threads、accept-count） | 🟠 高风险 | | |
| 7.3 | JVM 内存配置是否合理（线上至少 2G） | 🟠 高风险 | | |
| 7.4 | 是否开启了 access.log 且支持自动清理（rotate + max-days） | 🟡 中风险 | | |

---

## 八、数据库配置检查

> 目的：确保数据库连接配置合理，避免连接泄漏或性能问题。  
> **前提：生产配置门控已通过**（见上文"配置门控"）。未通过则 8.1–8.4 结论统一为 `⚠️ 待确认 - 缺生产配置`。
> AI 检查方式：读取 application.yaml 和线上 filter 配置中的 druid/datasource 配置。对于连接池大小，给出风险提示：需确认线上部署机器数，机器数 × 单机 max-active 不应超过 MySQL max_connections。

| # | 检查项 | 严重程度 | 结果 | 备注 |
|---|--------|---------|------|------|
| 8.1 | 数据库连接 URL 配置是否合理（时区、编码、zeroDateTimeBehavior 等） | 🟠 高风险 | | |
| 8.2 | 数据库连接池大小是否合理（单机一般不超过 100，需人工确认部署机器数以计算总连接数是否超过 MySQL max_connections） | 🟠 高风险 | | |
| 8.3 | 连接池超时和检测配置是否合理（max-wait、validation-query、test-while-idle） | 🟠 高风险 | | |
| 8.4 | 空闲连接回收策略是否合理（min-evictable-idle-time、time-between-eviction-runs） | 🟡 中风险 | | |

---

## 九、日志框架配置检查

> 目的：确保日志规范、可追踪、安全，支持线上问题排查。  
> **前提：生产配置门控已通过**（见上文"配置门控"）。未通过则 9.4 结论统一为 `⚠️ 待确认 - 缺生产配置`。

| # | 检查项 | 严重程度 | 结果 | 备注 |
|---|--------|---------|------|------|
| 9.1 | 是否使用统一日志框架（SLF4J + Logback） | 🟠 高风险 | | |
| 9.2 | 日志级别使用是否合理（线上 INFO，避免大量 DEBUG） | 🟠 高风险 | | |
| 9.3 | 日志格式是否结构化，支持检索和分析 | 🟡 中风险 | | |
| 9.4 | 日志中是否存在敏感信息输出（密码、密钥、身份证号等） | 🔴 阻断级 | | |
| 9.5 | 日志是否包含链路追踪信息（traceId） | 🟠 高风险 | | |
| 9.6 | 日志文件是否配置了滚动归档和自动清理（maxHistory、cleanHistoryOnStart） | 🟡 中风险 | | |

---

## 十、应用性能监控检查

> 目的：确保应用具备完善的性能监控能力，支持线上问题快速定位。  
> AI 检查方式：搜索 pom.xml 中是否引入 `metrics-ump-client`；搜索代码中 `@Metrics` 注解、`MetricsClient.newInstance(...)` 手动埋点、`MetricsLoggerFactory.init()` 初始化调用；检查新增/变更的 Controller 和关键业务类是否都有埋点覆盖。

| # | 检查项 | 严重程度 | 结果 | 备注 |
|---|--------|---------|------|------|
| 10.1 | 是否引入 metrics-ump-client SDK 并完成初始化（`MetricsLoggerFactory.init()`） | 🟠 高风险 | | |
| 10.2 | 所有对外接口是否有监控埋点（`@Metrics` 注解或 `MetricsClient` 手动埋点，覆盖 QPS/RT/SUCCESS_RATE） | 🟠 高风险 | | |
| 10.3 | 新增/变更的接口是否补充了监控埋点 | 🟠 高风险 | | |
| 10.4 | 关键业务流程是否有性能埋点（如存储操作、外部调用等） | 🟡 中风险 | | |
| 10.5 | 是否监控 JVM 性能指标（`MetricsLoggerFactory.init()` 采集 + JVM GC 日志参数） | 🟠 高风险 | | |

---

## 十一、业务指标监控检查

> 目的：确保核心业务指标可观测，异常可及时发现和告警。  
> AI 检查方式：核心业务指标统一为请求量（QPS）、耗时（RT）、成功率（SUCCESS_RATE）。检查所有 Controller/Service 是否通过 `@Metrics` 或 `MetricsClient` 覆盖了这三个指标。告警和实时统计配置在监控平台侧，代码中无法检查，标记为 `⚠️ 待确认，需查看监控平台`。

| # | 检查项 | 严重程度 | 结果 | 备注 |
|---|--------|---------|------|------|
| 11.1 | 所有接口是否监控了核心业务指标（QPS、RT、SUCCESS_RATE） | 🟠 高风险 | | |
| 11.2 | 新增/变更接口是否纳入监控 | 🟠 高风险 | | |
| 11.3 | 是否监控业务异常和错误分布（`sr_incrSuccess`/`sr_incrTotal` 机制） | 🟠 高风险 | | |
| 11.4 | 是否支持业务指标的实时统计和趋势分析（待确认，需查看监控平台） | 🟡 中风险 | | |
| 11.5 | 是否设置了业务指标的阈值告警（待确认，需查看监控平台） | 🟠 高风险 | | |

---

## 十二、代码安全检查

> 目的：确保代码不存在常见安全漏洞和敏感信息泄露风险。  
> **前提：生产配置门控已通过**（见上文"配置门控"）。未通过则 12.5 结论统一为 `⚠️ 待确认 - 缺生产配置`。
> AI 检查方式：搜索 MyBatis XML 中的 `${}` 拼接（排除 Generator 标准 Example 模式）；检查是否有 Java 原生反序列化（`ObjectInputStream`）；搜索明文密码/密钥/凭据；检查 Controller 鉴权注解覆盖；检查文件上传白名单和大小限制。

| # | 检查项 | 严重程度 | 结果 | 备注 |
|---|--------|---------|------|------|
| 12.1 | 是否存在 SQL 注入风险（MyBatis `${}` 拼接、字符串拼接 SQL） | 🔴 阻断级 | | |
| 12.2 | 是否存在 XSS 漏洞（用户输入未转义直接输出） | 🔴 阻断级 | | |
| 12.3 | 是否存在不安全的反序列化（Java 原生 `ObjectInputStream`） | 🔴 阻断级 | | |
| 12.4 | 第三方依赖是否存在已知漏洞（建议上线前执行 dependency-check 扫描） | 🟠 高风险 | | |
| 12.5 | 敏感数据是否加密存储（密码用 Jasypt `ENC(...)` 或 Druid `config.decrypt`，密钥/凭据不能明文硬编码） | 🔴 阻断级 | | |
| 12.6 | 接口是否有合理的鉴权（检查所有 Controller 是否有 `@OAuth` 或其他鉴权注解） | 🔴 阻断级 | | |
| 12.7 | 文件上传是否限制了类型和大小（白名单 + max-file-size） | 🟠 高风险 | | |
| 12.8 | 是否存在路径遍历风险（文件名是否由用户直接控制） | 🔴 阻断级 | | |

---

## 十三、默认账户和密码安全检查

> 目的：确保系统不存在默认凭据风险。

| # | 检查项 | 严重程度 | 结果 | 备注 |
|---|--------|---------|------|------|
| 13.1 | 默认账户是否已禁用或更改（如 Druid stat-view-servlet） | 🔴 阻断级 | | |
| 13.2 | 默认密码是否已修改为强密码 | 🔴 阻断级 | | |
| 13.3 | 密码策略是否满足安全要求（长度、复杂度，如 Jasypt 加密主密码不应过于简单） | 🟠 高风险 | | |

---

## 十四、回滚方案检查

> 目的：确保变更可回滚，降低上线失败的影响范围。  
> AI 检查方式：检查是否有数据库回滚脚本（DDL 目录下是否有对应的 rollback SQL）；检查配置是否通过 Git 管理可回滚；检查是否使用了配置中心（如 Typhon/Apollo）支持热回滚。14.4 和 14.5 需人工确认。

| # | 检查项 | 严重程度 | 结果 | 备注 |
|---|--------|---------|------|------|
| 14.1 | 是否制定了代码回滚方案（涉及核心组件切换时需确保可回退） | 🔴 阻断级 | | |
| 14.2 | 数据库变更是否支持回滚（是否有对应的回滚 DDL 脚本） | 🔴 阻断级 | | |
| 14.3 | 配置变更是否支持回滚（Git 管理 / 配置中心热回滚） | 🟠 高风险 | | |
| 14.4 | 是否明确了回滚触发条件和决策人（待人工确认） | 🟠 高风险 | | |
| 14.5 | 回滚方案是否经过验证（待人工确认） | 🟡 中风险 | | |

---

## 十五、降级评估模式

> 仅在用户明确 bypass 生产配置门控时激活。正常模式（门控通过）跳过本章。

### 15.1 模式说明

本次评估在生产配置缺失的情况下继续推进。下列章节的结论**仅基于代码静态扫描，不含配置值验证**，统一打标 `⚠️ 待确认 - 缺生产配置`：

| 章节 | 无法验证的内容 | 建议补做的人工确认 |
|------|---------------|------------------|
| 5.1 | 线上配置是否含 beta/sit/dev/test 关键字 | 部署负责人 grep 线上配置 |
| 5.2 | 线上网络拓扑是否隔离非生产 | 运维/SRE |
| 6.6 | Redis 密码是否加密存储 | 部署负责人检查线上配置 |
| 8.1–8.4 | DB 连接池、URL、超时配置值 | DBA + 部署负责人 |
| 9.4 | 日志中实际打印的字段（含敏感字段） | 灰度环境日志审计 |
| 12.5 | 密钥/凭据加密方式（Jasypt vs Druid） | 部署负责人 |

### 15.2 结论可信度声明

- 上述章节的 `✅ 通过` 视为不成立（即使代码看起来 OK，配置值未验证）。
- 用户接受 bypass 即视为接受"这些章节无证据"的风险。
- 评估总结的"⚠️ 待确认事项"小节必须**显式列出**降级模式激活的事实，并引用本节"建议补做"清单。
- 报告中可引用"十五、15.1"指向本章节。

---

## 评估总结

### 🔴 阻断级问题（必须处理才能上线）

| 编号 | 问题 | 建议 |
|------|------|------|
| | | |

### 🟠 高风险问题（强烈建议修复）

| 编号 | 问题 | 建议 |
|------|------|------|
| | | |

### 🟡 中风险问题（建议优化）

| 编号 | 问题 | 建议 |
|------|------|------|
| | | |

### 🔵 低风险问题（可后续改进）

| 编号 | 问题 | 建议 |
|------|------|------|
| | | |

### ⚠️ 待确认事项

| 编号 | 需确认内容 | 原因 | 建议确认方 |
|------|------------|------|------------|
| | | | |

### ✅ 正面改进

- （列出本次变更中值得肯定的改进点）
